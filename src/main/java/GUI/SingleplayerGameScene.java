package GUI;

import javafx.scene.Parent;

public class SingleplayerGameScene extends MultiplayerGameScene {
    public SingleplayerGameScene(double width, double height) {
        super(width, height);
    }
    //just add undo/Redo button to the base scene

}
